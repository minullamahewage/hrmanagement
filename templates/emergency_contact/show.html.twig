{% extends 'base.html.twig' %}

{% block title %}EmergencyContact{% endblock %}

{% block body %}
    <h1>EmergencyContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emergency_contact.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ emergency_contact.name }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ emergency_contact.telephone }}</td>
            </tr>
            <tr>
                <th>Emp_id</th>
                <td>{{ emergency_contact.empId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('emergency_contact_index') }}">back to list</a>

    <a href="{{ path('emergency_contact_edit', {'id': emergency_contact.id}) }}">edit</a>

    {{ include('emergency_contact/_delete_form.html.twig') }}
{% endblock %}
