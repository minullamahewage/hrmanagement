{% extends 'base.html.twig' %}

{% block title %}EmpData{% endblock %}

{% block body %}
    <h1>EmpData</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Attribute</th>
                <td>{{ emp_datum.attribute }}</td>
            </tr>
            <tr>
                <th>Emp_id</th>
                <td>{{ emp_datum.empId }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ emp_datum.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('emp_data_index') }}">back to list</a>

    <a href="{{ path('emp_data_edit', {'attribute': emp_datum.attribute}) }}">edit</a>

    {{ include('emp_data/_delete_form.html.twig') }}
{% endblock %}
