{% extends 'base.html.twig' %}

{% block title %}EmpTelephone{% endblock %}

{% block body %}
    <h1>EmpTelephone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Telephone</th>
                <td>{{ emp_telephone.telephone }}</td>
            </tr>
            <tr>
                <th>EmpId</th>
                <td>{{ emp_telephone.empId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('emp_telephone_index') }}">back to list</a>

    <a href="{{ path('emp_telephone_edit', {'telephone': emp_telephone.telephone}) }}">edit</a>

    {{ include('emp_telephone/_delete_form.html.twig') }}
{% endblock %}
