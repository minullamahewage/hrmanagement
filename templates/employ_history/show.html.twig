{% extends 'base.html.twig' %}

{% block title %}EmployHistory{% endblock %}

{% block body %}
    <h1>EmployHistory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>EmpHistoryId</th>
                <td>{{ employ_history.empHistoryId }}</td>
            </tr>
            <tr>
                <th>ToDate</th>
                <td>{{ employ_history.toDate ? employ_history.toDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FromDate</th>
                <td>{{ employ_history.fromDate ? employ_history.fromDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EmpStatus</th>
                <td>{{ employ_history.empStatus }}</td>
            </tr>
            <tr>
                <th>Emp_id</th>
                <td>{{ employ_history.empId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('employ_history_index') }}">back to list</a>

    <a href="{{ path('employ_history_edit', {'empHistoryId': employ_history.empHistoryId}) }}">edit</a>

    {{ include('employ_history/_delete_form.html.twig') }}
{% endblock %}
