{% extends 'base.html.twig' %}

{% block title %}JobTitle{% endblock %}

{% block body %}
    <h1>JobTitle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>JobTitle</th>
                <td>{{ job_title.jobTitle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job_title.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('job_title_index') }}">back to list</a>

    <a href="{{ path('job_title_edit', {'jobTitle': job_title.jobTitle}) }}">edit</a>

    {{ include('job_title/_delete_form.html.twig') }}
{% endblock %}
