<form method="post" action="{{ path('leave_type_delete', {'leaveType': leave_type.leaveType}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ leave_type.leaveType) }}">
    <button class="btn">Delete</button>
</form>
