<form method="post" action="{{ path('pay_grade_delete', {'payGrade': pay_grade.payGrade}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pay_grade.payGrade) }}">
    <button class="btn">Delete</button>
</form>
