{% extends 'base.html.twig' %}

{% block title %}Employees by Branch{% endblock %}

{% block body %}
    <h1>Employees by Branch</h1>
    <h2>{{ branch }} Branch <h2>
    <table class="table">
        <thead>
            <tr>
                <th>EmpId</th>
                <th>Nic</th>
                <th>Name</th>
                <th>Email</th>
                <th>PayGrade</th>
                <th>SupervisorId</th>
                <th>DeptId</th>
                <th>Employment Status</th>
                <th>Job Title</th>
            </tr>
        </thead>
        <tbody>
        {% for employee in employees %}
            <tr>
                <td>{{ employee.emp_id }}</td>
                <td>{{ employee.NIC }}</td>
                <td>{{ employee.name }}</td>
                <td>{{ employee.email }}</td>
                <td>{{ employee.pay_grade }}</td>
                <td>{{ employee.supervisor_id }}</td>
                <td>{{ employee.dept_id }}</td>
                <td>{{ employee.emp_status }}</td>
                <td>{{ employee.job_title }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="18">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('report_index') }}">back to report options</a>
    
{% endblock %}

